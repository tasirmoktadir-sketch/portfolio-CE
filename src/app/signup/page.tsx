
export default function SignupPage() {
  // This page is intentionally left blank to prevent new user signups from the UI.
  // The site administrator should create new users directly in the Firebase console.
  return null;
}
